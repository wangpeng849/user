eureka:
  client:
    service-url:
      defaultZone: http://localhost:8762/eureka/
#server:
#  port: 8081
spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
      profile: dev
  application:
    name: user


